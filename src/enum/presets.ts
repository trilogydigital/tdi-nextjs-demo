export enum PlaylistType {
  ContinueWatching = 'continue_watching',
  Favorites = 'favorites',
  Featured = 'featured',
  Playlist = 'playlist',
  Downloads = 'downloads',
  InLine = 'inline',
  TopTen = 'topTen',
  Live = 'live',
  Upcoming = 'upcoming',
}
